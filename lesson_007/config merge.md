# Настройка слияния

```bash
git merge --no--ff <ветка> # запрет fast-forward(быстрое слияние), всегда будет создаваться merge-commit
git merge --edit <ветка> # редактирования сообщения commit при слиянии
```